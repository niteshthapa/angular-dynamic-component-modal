import { ViewcontainerrefDirective } from './viewcontainerref.directive';

describe('ViewcontainerrefDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewcontainerrefDirective();
    expect(directive).toBeTruthy();
  });
});
